拓展周方案：基于 License 授权机制和多租户架构的类 SaaS 资产安全管理平台
===========================================

一、方向一：Python 客户端授权方案
--------------------

### （1）设计目标

解决 Python 客户端打包为 exe 后被任意复制使用的问题，实现功能分级和商业授权控制。

整体采用：

> 本地 License 校验 + 服务端最终控制

客户端负责界面展示和基础校验，核心能力由服务端统一控制。

* * *

### （2）授权流程

客户端首次激活时采集硬件特征：
* MAC 地址
* 硬盘序列号
* 主板序列号

组合生成机器指纹（MachineID）。

客户端向平台申请授权，平台使用 RSA 私钥生成 License 文件，客户端利用内置公钥验证签名。

License 内容包括：
* MachineID
* 授权版本
* 功能模块列表
* 主机数量限制
* 用户数量限制
* 到期时间
* 数字签名

形成：
    Python Client
          ↓
    生成 MachineID
          ↓
    请求授权
          ↓
    Platform Server
    （RSA私钥签名）
          ↓
    License
          ↓
    客户端使用RSA公钥验证

* * *

### （3）机器指纹容错机制

为避免更换部分硬件导致授权失效，采用组合匹配机制。

首次激活记录：
* MAC 地址
* 硬盘序列号
* 主板序列号

后续验证时采用：

> 三项特征中至少两项匹配即可通过。

兼顾防复制和硬件升级需求。

* * *

### （4）功能分级

### Trial（体验版）

* 主机基本信息
* 少量日志

#### Standard（标准版）

* 全部传统安全功能
* 多租户
* RBAC
* 审计
* 集中管理

#### Professional（智能版）

* AI 分析
* AI 修复建议
* AI 报告
* 更高资源配额

未授权或权限不足时，仅展示基础功能，高级功能点击后提示需要商业授权。

* * *

### （5）安全加固

客户端仅负责：
    采集
    ↓
    上传
    ↓
    展示结果

核心能力（漏洞分析、AI 分析、修复建议等）部署在服务端。

辅助保护措施：

* PyArmor 代码混淆；

* Cython 编译关键模块；

* 定期在线校验；

* Token 缓存；

* 时间异常检测。

检测到时间异常时，不直接锁死，而是要求重新联网验证授权。

* * *

二、方向二：类 SaaS 化改造方案
------------------

### （1）总体思路

平台支持多个企业同时使用，实现不同企业之间的数据隔离。

A 企业管理员只能查看和管理 A 企业的数据；

B 企业管理员只能查看和管理 B 企业的数据；

平台超级管理员能够管理所有租户。

* * *

### （2）多租户架构

采用：

> 共享数据库 + 共享 Schema + tenant_id

在所有业务表中增加：
    tenant_id

涉及全部业务数据。

该方案改动最小，适合当前项目阶段。

* * *

### （3）用户体系

新增：

#### tenant（企业表）

记录企业信息。

#### tenant_user（企业用户关系）

记录用户所属企业。

用户登录成功后，将 tenant_id 写入 JWT。

例如（仅示例格式，实际字段按真实数据库表来）：
    {
      "userId": 1,
      "tenantId": 1001
    }

* * *

### （4）数据隔离机制

整体流程：
    JWT
    ↓
    SecurityContext
    ↓
    TenantContextHolder
    ↓
    业务层
    ↓
    数据库

当前阶段通过统一的租户上下文获取 tenant_id，保证所有业务操作均携带当前租户信息。

后续可升级为：
    JWT
    ↓
    SecurityContext
    ↓
    TenantContextHolder
    ↓
    TenantInterceptor
    ↓
    自动 SQL 重写

实现真正的自动数据隔离。

* * *

### （5）消息队列隔离

客户端不传递 tenant_id。

消息只携带：
    agent_id / host_uuid（示例字段，实际字段按照真实数据库表来）

消费端收到消息后：
    RabbitMQ
    ↓
    Consumer
    ↓
    根据 agent_id 查询 host 表
    ↓
    获取 tenant_id
    ↓
    写入数据库

由服务端自动完成租户映射。

避免客户端伪造 tenant_id 导致跨租户数据污染。

* * *

### （6）管理体系

系统包含两个视角。

#### 平台超级管理员

负责：

* 创建企业；

* 分配 License；

* 查看所有租户；

* 统计系统资源；

* 管理授权策略。

#### 企业管理员

负责：

* 管理本企业用户；

* 管理本企业 Agent；

* 查看本企业资产。

普通用户仅拥有查看权限。

* * *

三、License 与多租户结合
----------------

新增 License 表：
    license
    (
        license_key,
        tenant_id,
        edition,
        host_limit,
        user_limit,
        expire_time
    )

例如：

A 企业购买：

* Professional 版本；

* 100 台主机；

* 20 个用户；

* 有效期一年。

系统自动完成：
    License
    ↓
    Tenant
    ↓
    Host
    ↓
    User
    ↓
    Role
    ↓
    Permission

当接入主机数量超过限制时：
    当前授权最多允许接入 100 台主机，
    请联系管理员升级授权。

* * *

四、总体架构
------

                    Super Admin
                          │
                    License Center
                          │
              ┌───────────┼───────────┐
              │           │           │
           Tenant A    Tenant B    Tenant C
              │           │           │
         User、Host、Asset、Log
              │
          Python Agent

五、方案核心思想
--------

通过 License 授权机制解决“谁能使用系统”的问题，通过多租户机制解决“谁的数据归谁”的问题。

在保持现有 Spring Boot + Python + RabbitMQ 架构不变的基础上，实现平台向企业级、类 SaaS 化方向演进，为后续商业化扩展提供基础。


# 6.15 22：22调整下一步实现顺序

* 调整实现顺序。

* 下一步先完成漏洞域。

* 然后完成补丁域（不包含 MQ 路径）。

* 再完成基线域。

* 完成后进入 MQ tenant 映射阶段。

* MQ tenant 映射完成后，再处理 User + RBAC。

* 最后再做超级管理员后台、License 中心和客户端授权。


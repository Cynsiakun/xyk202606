5.agent客户端的强化-可在客户端上面操作
	5.1增加客户端的功能（客户端可以离线操作，可以操作哪些自己定，上线之后将操作记录同步到平台）
	5.2操作界面可以向成熟的安全软件去靠拢
6.数据统计，态势显示
	6.1平台登录之后的首页：主要需要展示一些统计图表（以最快的速度直观地展示我们的平台的数据价值）
	6.2态势显示核心主要以动态图的形式展示对应资产的安全问题
		态势显示的价值是什么？给领导检查时看的
7.agent的多操作系统的支持
	7.1 支持Windows
7.2 支持Linux（基线规则也不一样）（无操作页面）


# 客户端制定对任务五5的计划
5.agent客户端的强化-可在客户端上面操作
	5.1增加客户端的功能（客户端可以离线操作，可以操作哪些自己定，上线之后将操作记录同步到平台）
	5.2操作界面可以向成熟的安全软件去靠拢
	5.3 当前阶段收敛方案（最小改动原则）
		- 客户端职责：执行 + 本地缓存 + 离线同步 + 原始结果展示，不做规则判定、风险分析、态势分析。
		- 当前不支持：基线加固、基线回滚。
		- 当前支持的客户端本地操作：
			- 主机信息采集
			- 补丁信息采集
			- 资产信息采集（账号 / 服务 / 进程 / APP）
			- 端口扫描
			- 基线检测（仅检测，不判定）
			- 待同步记录手动同步
		- 设计原则：
			- 复用现有采集 / 检测代码，不重写业务能力；
			- 本地先落库，再执行，再更新结果，再异步同步；
			- 客户端界面只展示“做了什么、成功没、结果是什么、是否已同步”。
	5.4 客户端本地SQLite设计（单表）
		- 目的：
			- 支撑离线操作不丢失；
			- 支撑程序重启后继续同步；
			- 支撑界面展示操作记录、执行状态、同步状态。
		- 表结构建议：

```sql
CREATE TABLE operation_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    operation_id TEXT NOT NULL UNIQUE,
    type TEXT NOT NULL,
    status TEXT NOT NULL,
    request_json TEXT NOT NULL,
    result_json TEXT,
    sync_status TEXT NOT NULL DEFAULT 'PENDING',
    created_at TEXT NOT NULL,
    started_at TEXT,
    finished_at TEXT,
    synced_at TEXT,
    last_error TEXT
);

CREATE INDEX idx_operation_record_status
    ON operation_record(status, created_at DESC);

CREATE INDEX idx_operation_record_sync_status
    ON operation_record(sync_status, created_at DESC);
```

		- 字段说明：
			- operation_id：一次客户端操作的唯一ID，建议 UUID。
			- type：操作类型，建议值：
				- host_info
				- patch_scan
				- asset_scan
				- port_scan
				- baseline_scan
			- status：本地执行状态，建议值：
				- PENDING
				- RUNNING
				- SUCCESS
				- ERROR
			- request_json：本次操作的请求参数或任务参数。
			- result_json：本次操作的原始结果全文，失败时也记录错误信息。
			- sync_status：同步状态，建议值：
				- PENDING
				- SYNCED
				- FAILED
			- created_at / started_at / finished_at / synced_at：时间戳。
			- last_error：最近一次执行失败或同步失败原因。
	5.5 客户端与平台端通信设计（统一结果通道）
		- 原则：
			- 不再按业务拆多个结果 exchange；
			- 客户端本地触发的结果、离线补同步的结果，统一发往同一结果通道；
			- 具体业务类型由 JSON 中的 type 字段区分。
		- MQ建议：
			- Exchange：result_exchange
			- Queue：agent_result_queue
			- Routing Key：agent_result
		- 消息统一外壳：

```json
{
  "type": "asset_scan",
  "operationId": "9d7c8d33-42be-4c49-8c3c-7f0fbe1d9c21",
  "hostName": "DESKTOP-ABC123",
  "macAddress": "00:1A:2B:3C:4D:5E",
  "status": "SUCCESS",
  "request": {
    "account": 1,
    "service": 1,
    "process": 0,
    "app": 1
  },
  "result": {
    "accounts": [],
    "services": [],
    "apps": []
  },
  "createdAt": "2026-06-18T14:20:00+08:00",
  "finishedAt": "2026-06-18T14:20:08+08:00"
}
```

		- 字段说明：
			- type：业务类型，建议值：
				- host_info
				- patch_scan
				- asset_scan
				- port_scan
				- baseline_scan
			- operationId：客户端本地操作唯一ID，对应本地 operation_record.operation_id。
			- hostName / macAddress：主机标识。
			- status：本次操作执行结果，SUCCESS 或 ERROR。
			- request：本次操作入参。
			- result：本次操作原始结果，不做客户端二次分析。
			- createdAt / finishedAt：操作开始与结束时间。
		- 各 type 的 request / result 约定：
			- host_info
				- request：可为空对象 {}
				- result：主机基础信息 JSON
			- patch_scan
				- request：可为空对象 {}
				- result：补丁采集结果 JSON
			- asset_scan
				- request：账号 / 服务 / 进程 / APP 的勾选参数
				- result：可包含 accounts / services / processes / apps 四类数组
			- port_scan
				- request：scanRange / customPorts / grabBanner
				- result：开放端口列表及扫描原始结果
			- baseline_scan
				- request：本次检测模板或任务 JSON
				- result：原始检测结果 results[]
	5.6 客户端执行流程
		- 用户在客户端界面点击某个操作；
		- 客户端先写入 operation_record，状态置为 PENDING；
		- 开始执行时改为 RUNNING；
		- 执行完成后写入 result_json，并更新为 SUCCESS 或 ERROR；
		- 若当前可连平台，则立即发送到 result_exchange；
		- 若发送成功，则 sync_status 改为 SYNCED；
		- 若发送失败，则 sync_status 改为 FAILED 或保持 PENDING，等待后续手动 / 自动重试；
		- 客户端启动时和用户点击“立即同步待同步记录”时，扫描本地 sync_status != SYNCED 的记录并补发。
	5.7 客户端界面设计
		- 总体定位：
			- 客户端是“操作台”，不是“分析台”。
			- 不展示风险评分、规则解释、态势图、合规率。
		- 左侧：主机状态
			- 主机名
			- IP
			- MAC
			- 操作系统
			- MQ状态
			- 最后心跳
			- 待同步数量
		- 中间：手动操作区
			- 主机信息
				- 按钮：立即采集
			- 补丁信息
				- 按钮：立即采集
			- 资产信息
				- 勾选项：账号 / 服务 / 进程 / APP
				- 按钮：开始采集
			- 端口扫描
				- 选项：常用端口 / 全量端口 / 自定义
				- 输入框：自定义端口
				- 勾选项：抓取 Banner
				- 按钮：开始扫描
			- 基线检测
				- 下拉框：检测模板
				- 按钮：开始检测
			- 同步
				- 按钮：立即同步待同步记录
		- 右侧：记录与结果
			- 上半部分：操作记录列表
				- 时间
				- 类型
				- 状态
				- 同步状态
			- 下半部分：原始结果详情
				- request_json
				- result_json
		- 明确不展示：
			- 风险等级
			- 规则判定结果统计
			- 平台态势能力
			- 基线PASS/FAIL汇总图
	5.8 与平台端对接要求（需明确配合）
		- 平台端需提供统一结果接收通道：
			- Exchange：agent_result_exchange
			- Queue：agent_result_queue
			- Routing Key：agent_result
		- 平台端需按 JSON.type 区分业务，不再依赖不同 exchange 区分不同采集类型。
		- 平台端需支持以下 type：
			- host_info
			- patch_scan
			- asset_scan
			- port_scan
			- baseline_scan
		- 平台端接收后需按 type 分发处理：
			- host_info：进入主机基础信息处理逻辑
			- patch_scan：进入补丁处理逻辑
			- asset_scan：拆分或保存账号 / 服务 / 进程 / APP 结果
			- port_scan：进入端口扫描结果处理逻辑
			- baseline_scan：进入基线检测原始结果处理逻辑，由平台端完成最终规则判定
		- 平台端需以 operationId 做幂等控制，避免客户端离线重试导致重复入库。
		- 平台端需兼容客户端上传的原始 request / result JSON，不要求客户端先做格式裁剪或规则解释。
		- 平台端需保留客户端原始结果，供平台后续审计、对账、重放或排错使用。
		- 平台端对 baseline_scan 的要求：
			- 客户端只上传原始检测结果；
			- PASS / FAIL / ERROR / 风险等级 / 合规统计，统一由平台端计算；
			- 平台端不要要求客户端输出本地判定图表。
		- 平台端对客户端基线检测模板的要求：
			- 若客户端基线检测来源于平台模板，则平台端需提供可下发 / 可缓存的 baseline_scan 任务 JSON；
			- 客户端本地执行时直接复用该 JSON，不新增客户端私有规则语义。
		- 平台联调重点：
			- agent_result_exchange 是否已创建并正确绑定 agent_result_queue；
			- 平台是否能按 type 正确解析统一消息；
			- operationId 重试幂等是否生效；
			- 断网后补同步是否不会造成重复脏数据；
			- baseline_scan 原始结果是否能被平台正常判定。
6.数据统计，态势显示
	6.1平台登录之后的首页：主要需要展示一些统计图表（以最快的速度直观地展示我们的平台的数据价值）
	6.2态势显示核心主要以动态图的形式展示对应资产的安全问题
		态势显示的价值是什么？给领导检查时看的
7.agent的多操作系统的支持
	7.1 支持Windows
	7.2 支持Linux（基线规则也不一样）（无操作页面）

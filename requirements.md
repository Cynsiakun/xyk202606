# 合规基线模块需求


目标：

实现 Windows 合规基线检查、统计、自动加固、人工工单闭环。

关于合规基线的功能要求：
1.规则管理
2.基线检测(按主机/按批次/定时)
3.安全加固(把检测出来有问题的主机进行修复：手动修复/自动修复)
4.关于手动加固的项(必须人工检测) 应该怎么处理 (工单的派遣)
5.结果导出
6.主机视角的合规基线的可视化

流程：

平台
↓
下发任务
↓
Agent执行脚本
↓
返回JSON
↓
规则引擎
↓
结果
↓
自动修复
↓
工单
↓
复检

我们这边做的是平台端。

数据库：

baseline_rule
baseline_rule_item
...
共10张表

数据库连接地址：127.0.0.1:3306，用户名：root，密码：root，数据库名：xyk2026。
跟合规基线模块相关的数据库表名和数据结构见database.md。

消息队列：
rabbitMQ
消息队列连接地址：139.155.139.242:15333，用户名：admin，密码：admin。
1.平台端下发任务：
复用之前下发指令的：
交换机：agent_exchange
队列：agent_{mac地址}_queue
路由键：{mac地址}
2.客户端返回处理结果：
交换机：sysinfo_exchange；
队列：baseline_queue；
路由键：baseline

支持：

- 手动任务
- 定时任务
- 自动修复
- 配置备份
- 回滚
- 工单
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.ServiceMapper">

    <resultMap id="ServiceResultMap" type="com.cd.entity.ServiceEntity">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="host_name" property="hostName"/>
        <result column="mac_address" property="macAddress"/>
        <result column="asset_count" property="assetCount"/>
        <result column="asset_json" property="assetJson"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, task_id, host_name, mac_address, asset_count, asset_json, deleted, created_at, updated_at
    </sql>

    <sql id="listWhere">
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.ServiceEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO services (task_id, host_name, mac_address, asset_count, asset_json)
        VALUES (#{taskId}, #{hostName}, #{macAddress}, #{assetCount}, #{assetJson})
    </insert>

    <select id="selectById" resultMap="ServiceResultMap">
        SELECT <include refid="baseColumns"/>
        FROM services
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectPage" resultMap="ServiceResultMap">
        SELECT <include refid="baseColumns"/>
        FROM services
        <include refid="listWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countFiltered" resultType="long">
        SELECT COUNT(1) FROM services
        <include refid="listWhere"/>
    </select>

    <update id="softDeleteById">
        UPDATE services SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectLatestPerMac" resultMap="ServiceResultMap">
        SELECT <include refid="baseColumns"/>
        FROM services a
        WHERE a.deleted = 0
          AND a.id = (SELECT MAX(id) FROM services WHERE mac_address = a.mac_address AND deleted = 0)
    </select>
</mapper>

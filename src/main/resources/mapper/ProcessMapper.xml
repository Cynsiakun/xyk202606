<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.ProcessMapper">

    <resultMap id="ProcessResultMap" type="com.cd.entity.ProcessEntity">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="task_id" property="taskId"/>
        <result column="host_name" property="hostName"/>
        <result column="mac_address" property="macAddress"/>
        <result column="source" property="source"/>
        <result column="asset_count" property="assetCount"/>
        <result column="asset_json" property="assetJson"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, tenant_id, task_id, host_name, mac_address, source, asset_count, asset_json, deleted, created_at, updated_at
    </sql>

    <sql id="listWhere">
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <sql id="listWhereByTenant">
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.ProcessEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO processes (tenant_id, task_id, host_name, mac_address, source, asset_count, asset_json)
        VALUES (COALESCE(#{tenantId}, 0), #{taskId}, #{hostName}, #{macAddress}, #{source}, #{assetCount}, #{assetJson})
    </insert>

    <select id="selectById" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByIdAndTenant" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </select>

    <select id="selectPage" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        <include refid="listWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectPageByTenant" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        <include refid="listWhereByTenant"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countFiltered" resultType="long">
        SELECT COUNT(1) FROM processes
        <include refid="listWhere"/>
    </select>

    <select id="countFilteredByTenant" resultType="long">
        SELECT COUNT(1) FROM processes
        <include refid="listWhereByTenant"/>
    </select>

    <update id="softDeleteById">
        UPDATE processes SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

    <update id="softDeleteByIdAndTenant">
        UPDATE processes
        SET deleted = 1
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

    <update id="updateAssetJsonById">
        UPDATE processes
        SET asset_json = #{assetJson},
            asset_count = #{assetCount}
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateAssetJsonByIdAndTenant">
        UPDATE processes
        SET asset_json = #{assetJson},
            asset_count = #{assetCount}
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

    <select id="selectLatestPerMac" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes a
        WHERE a.deleted = 0
          AND a.id = (SELECT MAX(id) FROM processes WHERE mac_address = a.mac_address AND deleted = 0)
    </select>

    <select id="selectLatestPerMacByTenant" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes a
        WHERE a.deleted = 0
          AND a.tenant_id = #{tenantId}
          AND a.id = (
              SELECT MAX(id)
              FROM processes
              WHERE mac_address = a.mac_address
                AND tenant_id = #{tenantId}
                AND deleted = 0
          )
    </select>

    <select id="selectLatestByMac" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        WHERE deleted = 0
          AND mac_address = #{macAddress}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectLatestByMacAndTenant" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        WHERE deleted = 0
          AND mac_address = #{macAddress}
          AND tenant_id = #{tenantId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectLatestNonEmptyByMacAndTenant" resultMap="ProcessResultMap">
        SELECT <include refid="baseColumns"/>
        FROM processes
        WHERE deleted = 0
          AND mac_address = #{macAddress}
          AND tenant_id = #{tenantId}
          AND COALESCE(asset_count, 0) > 0
        ORDER BY id DESC
        LIMIT 1
    </select>
</mapper>

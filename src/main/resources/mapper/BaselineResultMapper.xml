<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.BaselineResultMapper">

    <insert id="insert" parameterType="com.cd.entity.BaselineResultEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO baseline_result (task_id, task_host_id, host_id, rule_id, rule_version, check_key,
                                     status, actual_value, expected_value, message, evidence,
                                     remediation_status, scan_time, create_time)
        VALUES (#{taskId}, #{taskHostId}, #{hostId}, #{ruleId}, #{ruleVersion}, #{checkKey},
                #{status}, #{actualValue}, #{expectedValue}, #{message}, #{evidence},
                #{remediationStatus}, #{scanTime}, #{createTime})
    </insert>

    <select id="countByTaskHostId" resultType="int">
        SELECT COUNT(*) FROM baseline_result WHERE task_host_id = #{taskHostId}
    </select>

    <select id="selectByIds" resultType="com.cd.entity.BaselineResultEntity">
        SELECT id                 AS id,
               task_id            AS taskId,
               task_host_id       AS taskHostId,
               host_id            AS hostId,
               rule_id            AS ruleId,
               rule_version       AS ruleVersion,
               check_key          AS checkKey,
               status             AS status,
               actual_value       AS actualValue,
               expected_value     AS expectedValue,
               message            AS message,
               evidence           AS evidence,
               remediation_status AS remediationStatus,
               scan_time          AS scanTime,
               create_time        AS createTime
        FROM baseline_result
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectLatestBefore" resultType="com.cd.entity.BaselineResultEntity">
        SELECT id                 AS id,
               task_id            AS taskId,
               task_host_id       AS taskHostId,
               host_id            AS hostId,
               rule_id            AS ruleId,
               rule_version       AS ruleVersion,
               check_key          AS checkKey,
               status             AS status,
               actual_value       AS actualValue,
               expected_value     AS expectedValue,
               message            AS message,
               evidence           AS evidence,
               remediation_status AS remediationStatus,
               scan_time          AS scanTime,
               create_time        AS createTime
        FROM baseline_result
        WHERE host_id = #{hostId}
          AND rule_id = #{ruleId}
          AND COALESCE(check_key, '') = COALESCE(#{checkKey}, '')
          AND id &lt; #{beforeId}
        ORDER BY scan_time DESC, id DESC
        LIMIT 1
    </select>

    <update id="updateRemediationStatus">
        UPDATE baseline_result
        SET remediation_status = #{remediationStatus}
        WHERE id = #{id}
    </update>

    <update id="updateRemediationStatusByIds">
        UPDATE baseline_result
        SET remediation_status = #{remediationStatus}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>

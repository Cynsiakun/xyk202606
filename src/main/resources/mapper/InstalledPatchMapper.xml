<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.InstalledPatchMapper">

    <resultMap id="InstalledPatchResultMap" type="com.cd.entity.InstalledPatchEntity">
        <id column="id" property="id"/>
        <result column="host_id" property="hostId"/>
        <result column="patch_id" property="patchId"/>
        <result column="patch_type" property="patchType"/>
        <result column="product_name" property="productName"/>
        <result column="product_version" property="productVersion"/>
        <result column="install_time" property="installTime"/>
        <result column="install_status" property="installStatus"/>
        <result column="source" property="source"/>
        <result column="signature_status" property="signatureStatus"/>
        <result column="reboot_required" property="rebootRequired"/>
        <result column="superseded_by" property="supersededBy"/>
        <result column="is_security_patch" property="isSecurityPatch"/>
        <result column="raw_data" property="rawData"/>
        <result column="scan_time" property="scanTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.cd.entity.InstalledPatchEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO installed_patch (
            host_id, patch_id, patch_type, product_name, product_version,
            install_time, install_status, source, signature_status,
            reboot_required, superseded_by, is_security_patch, raw_data,
            scan_time
        ) VALUES (
            #{hostId}, #{patchId}, #{patchType}, #{productName}, #{productVersion},
            #{installTime}, #{installStatus}, #{source}, #{signatureStatus},
            #{rebootRequired}, #{supersededBy}, #{isSecurityPatch}, #{rawData},
            #{scanTime}
        )
    </insert>

    <update id="updateById" parameterType="com.cd.entity.InstalledPatchEntity">
        UPDATE installed_patch
        SET host_id = #{hostId},
            patch_id = #{patchId},
            patch_type = #{patchType},
            product_name = #{productName},
            product_version = #{productVersion},
            install_time = #{installTime},
            install_status = #{installStatus},
            source = #{source},
            signature_status = #{signatureStatus},
            reboot_required = #{rebootRequired},
            superseded_by = #{supersededBy},
            is_security_patch = #{isSecurityPatch},
            raw_data = #{rawData},
            scan_time = #{scanTime}
        WHERE id = #{id}
    </update>

    <select id="selectLatestByHostIdAndPatchId" resultMap="InstalledPatchResultMap">
        SELECT *
        FROM installed_patch
        WHERE host_id = #{hostId}
          AND patch_id = #{patchId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <delete id="deleteByHostIdAndPatchIdExcludeId">
        DELETE FROM installed_patch
        WHERE host_id = #{hostId}
          AND patch_id = #{patchId}
          AND id &lt;&gt; #{excludeId}
    </delete>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.SecurityAlertMapper">

    <insert id="insertBatch">
        INSERT IGNORE INTO security_alerts
            (tenant_id, source_log_id, host_id, event_id, rule_code, dedup_key,
             alert_name, level, risk_score, description, evidence_json, status, event_time)
        VALUES
        <foreach collection="list" item="it" separator=",">
            (COALESCE(#{it.tenantId}, 0), #{it.sourceLogId}, #{it.hostId}, #{it.eventId}, #{it.ruleCode}, #{it.dedupKey},
             #{it.alertName}, #{it.level}, #{it.riskScore}, #{it.description},
             #{it.evidenceJson}, #{it.status}, #{it.eventTime})
        </foreach>
    </insert>

    <select id="selectExistingDedupKeys" resultType="string">
        SELECT DISTINCT dedup_key
        FROM security_alerts
        WHERE status = 'new'
          AND create_time <![CDATA[>=]]> #{since}
          AND dedup_key IN
        <foreach collection="dedupKeys" item="it" open="(" separator="," close=")">
            #{it}
        </foreach>
    </select>
</mapper>

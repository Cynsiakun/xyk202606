<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.HostPatchRiskMapper">

    <resultMap id="HostPatchRiskResultMap" type="com.cd.entity.HostPatchRiskEntity">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="host_id" property="hostId"/>
        <result column="risk_id" property="riskId"/>
        <result column="risk_type" property="riskType"/>
        <result column="risk_name" property="riskName"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="related_patch_id" property="relatedPatchId"/>
        <result column="status" property="status"/>
        <result column="scan_time" property="scanTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="related_cve" property="relatedCve"/>
        <result column="evidence" property="evidence"/>
        <result column="recommendation" property="recommendation"/>
    </resultMap>

    <insert id="insert" parameterType="com.cd.entity.HostPatchRiskEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO host_patch_risk (
            tenant_id, host_id, risk_id, risk_type, risk_name, risk_level,
            related_patch_id, status, scan_time, related_cve,
            evidence, recommendation
        ) VALUES (
            COALESCE(#{tenantId}, 0), #{hostId}, #{riskId}, #{riskType}, #{riskName}, #{riskLevel},
            #{relatedPatchId}, #{status}, #{scanTime}, #{relatedCve},
            #{evidence}, #{recommendation}
        )
    </insert>

    <update id="updateById" parameterType="com.cd.entity.HostPatchRiskEntity">
        UPDATE host_patch_risk
        SET host_id = #{hostId},
            risk_type = #{riskType},
            risk_name = #{riskName},
            risk_level = #{riskLevel},
            related_patch_id = #{relatedPatchId},
            status = #{status},
            scan_time = #{scanTime},
            related_cve = #{relatedCve},
            evidence = #{evidence},
            recommendation = #{recommendation}
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
    </update>

    <select id="selectByRiskId" resultMap="HostPatchRiskResultMap">
        SELECT *
        FROM host_patch_risk
        WHERE risk_id = #{riskId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByRiskIdAndTenant" resultMap="HostPatchRiskResultMap">
        SELECT *
        FROM host_patch_risk
        WHERE risk_id = #{riskId}
          AND tenant_id = #{tenantId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <update id="markFixedByHostIdExceptRiskIds">
        UPDATE host_patch_risk
        SET status = 'fixed',
            updated_at = CURRENT_TIMESTAMP
        WHERE host_id = #{hostId}
          AND status = 'open'
        <choose>
            <when test="riskIds != null and riskIds.size() > 0">
                AND risk_id NOT IN
                <foreach collection="riskIds" item="riskId" open="(" separator="," close=")">
                    #{riskId}
                </foreach>
            </when>
            <otherwise>
                AND 1 = 1
            </otherwise>
        </choose>
    </update>

    <update id="markFixedByHostIdExceptRiskIdsAndTenant">
        UPDATE host_patch_risk
        SET status = 'fixed',
            updated_at = CURRENT_TIMESTAMP
        WHERE host_id = #{hostId}
          AND tenant_id = #{tenantId}
          AND status = 'open'
        <choose>
            <when test="riskIds != null and riskIds.size() > 0">
                AND risk_id NOT IN
                <foreach collection="riskIds" item="riskId" open="(" separator="," close=")">
                    #{riskId}
                </foreach>
            </when>
            <otherwise>
                AND 1 = 1
            </otherwise>
        </choose>
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.LicenseMapper">

    <resultMap id="LicenseEntityResultMap" type="com.cd.entity.LicenseEntity">
        <id column="id" property="id"/>
        <result column="license_key" property="licenseKey"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="edition" property="edition"/>
        <result column="host_limit" property="hostLimit"/>
        <result column="user_limit" property="userLimit"/>
        <result column="expire_time" property="expireTime"/>
        <result column="machine_id" property="machineId"/>
        <result column="signature" property="signature"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, license_key, tenant_id, edition, host_limit, user_limit, expire_time, machine_id, signature, status, created_at
    </sql>

    <insert id="insert" parameterType="com.cd.entity.LicenseEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO license (license_key, tenant_id, edition, host_limit, user_limit, expire_time, machine_id, signature, status)
        VALUES (#{licenseKey}, #{tenantId}, #{edition}, #{hostLimit}, #{userLimit}, #{expireTime}, #{machineId}, #{signature}, #{status})
    </insert>

    <update id="updateById" parameterType="com.cd.entity.LicenseEntity">
        UPDATE license
        SET license_key = #{licenseKey},
            tenant_id = #{tenantId},
            edition = #{edition},
            host_limit = #{hostLimit},
            user_limit = #{userLimit},
            expire_time = #{expireTime},
            machine_id = #{machineId},
            signature = #{signature},
            status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateMachineAndSignature">
        UPDATE license
        SET machine_id = #{machineId},
            signature = #{signature}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM license WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="LicenseEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license
        WHERE id = #{id}
    </select>

    <select id="selectByLicenseKey" resultMap="LicenseEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license
        WHERE license_key = #{licenseKey}
    </select>

    <select id="selectEffectiveByTenantId" resultMap="LicenseEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license
        WHERE tenant_id = #{tenantId}
          AND status = 1
          AND expire_time &gt; NOW()
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByTenantId" resultMap="LicenseEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license
        WHERE tenant_id = #{tenantId}
        ORDER BY id DESC
    </select>
</mapper>

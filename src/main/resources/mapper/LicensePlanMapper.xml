<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.LicensePlanMapper">

    <resultMap id="LicensePlanEntityResultMap" type="com.cd.entity.LicensePlanEntity">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="user_limit" property="userLimit"/>
        <result column="host_limit" property="hostLimit"/>
        <result column="feature_flags" property="featureFlags"/>
        <result column="status" property="status"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, code, name, user_limit, host_limit, feature_flags, status, sort_order, created_at, updated_at
    </sql>

    <select id="selectByCode" resultMap="LicensePlanEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license_plan
        WHERE code = #{code}
          AND status = 1
        LIMIT 1
    </select>

    <select id="selectEnabled" resultMap="LicensePlanEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM license_plan
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>
</mapper>

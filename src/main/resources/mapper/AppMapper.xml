<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.AppMapper">

    <resultMap id="AppResultMap" type="com.cd.entity.AppEntity">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="host_name" property="hostName"/>
        <result column="mac_address" property="macAddress"/>
        <result column="asset_count" property="assetCount"/>
        <result column="asset_json" property="assetJson"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, task_id, host_name, mac_address, asset_count, asset_json, deleted, created_at, updated_at
    </sql>

    <sql id="listWhere">
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.AppEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO apps (task_id, host_name, mac_address, asset_count, asset_json)
        VALUES (#{taskId}, #{hostName}, #{macAddress}, #{assetCount}, #{assetJson})
    </insert>

    <select id="selectById" resultMap="AppResultMap">
        SELECT <include refid="baseColumns"/>
        FROM apps
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectPage" resultMap="AppResultMap">
        SELECT <include refid="baseColumns"/>
        FROM apps
        <include refid="listWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countFiltered" resultType="long">
        SELECT COUNT(1) FROM apps
        <include refid="listWhere"/>
    </select>

    <update id="softDeleteById">
        UPDATE apps SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectLatestPerMac" resultMap="AppResultMap">
        SELECT <include refid="baseColumns"/>
        FROM apps a
        WHERE a.deleted = 0
          AND a.id = (SELECT MAX(id) FROM apps WHERE mac_address = a.mac_address AND deleted = 0)
    </select>
</mapper>

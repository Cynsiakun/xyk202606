<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.HostMapper">

    <resultMap id="HostEntityResultMap" type="com.cd.entity.HostEntity">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="hostname" property="hostname"/>
        <result column="ipv4" property="ipv4"/>
        <result column="mac_address" property="macAddress"/>
        <result column="os_name" property="osName"/>
        <result column="os_version" property="osVersion"/>
        <result column="os_arch" property="osArch"/>
        <result column="os_release" property="osRelease"/>
        <result column="cpu_model" property="cpuModel"/>
        <result column="cpu_physical_cores" property="cpuPhysicalCores"/>
        <result column="cpu_logical_cores" property="cpuLogicalCores"/>
        <result column="mem_total" property="memTotal"/>
        <result column="mem_used" property="memUsed"/>
        <result column="mem_available" property="memAvailable"/>
        <result column="mem_usage" property="memUsage"/>
        <result column="status" property="status"/>
        <result column="last_scan_time" property="lastScanTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, tenant_id, hostname, ipv4, mac_address, os_name, os_version, os_arch, os_release,
        cpu_model, cpu_physical_cores, cpu_logical_cores,
        mem_total, mem_used, mem_available, mem_usage,
        status, last_scan_time, created_at, updated_at
    </sql>

    <sql id="keywordWhere">
        <if test="keyword != null and keyword != ''">
            WHERE hostname LIKE CONCAT('%', #{keyword}, '%')
               OR ipv4 LIKE CONCAT('%', #{keyword}, '%')
               OR mac_address LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </sql>

    <sql id="keywordAnd">
        <if test="keyword != null and keyword != ''">
            AND (hostname LIKE CONCAT('%', #{keyword}, '%')
                 OR ipv4 LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.HostEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hosts (tenant_id, hostname, ipv4, mac_address, os_name, os_version, os_arch, os_release,
                           cpu_model, cpu_physical_cores, cpu_logical_cores,
                           mem_total, mem_used, mem_available, mem_usage, status, last_scan_time)
        VALUES (COALESCE(#{tenantId}, 0), #{hostname}, #{ipv4}, #{macAddress}, #{osName}, #{osVersion}, #{osArch}, #{osRelease},
                #{cpuModel}, #{cpuPhysicalCores}, #{cpuLogicalCores},
                #{memTotal}, #{memUsed}, #{memAvailable}, #{memUsage}, #{status}, #{lastScanTime})
    </insert>

    <update id="updateById" parameterType="com.cd.entity.HostEntity">
        UPDATE hosts
        SET hostname = #{hostname},
            ipv4 = #{ipv4},
            mac_address = #{macAddress},
            os_name = #{osName},
            os_version = #{osVersion},
            os_arch = #{osArch},
            os_release = #{osRelease},
            cpu_model = #{cpuModel},
            cpu_physical_cores = #{cpuPhysicalCores},
            cpu_logical_cores = #{cpuLogicalCores},
            mem_total = #{memTotal},
            mem_used = #{memUsed},
            mem_available = #{memAvailable},
            mem_usage = #{memUsage},
            status = #{status},
            last_scan_time = #{lastScanTime}
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
    </update>

    <delete id="deleteById">
        DELETE FROM hosts WHERE id = #{id}
    </delete>

    <delete id="deleteByIdAndTenant">
        DELETE FROM hosts
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
    </delete>

    <select id="selectById" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE id = #{id}
    </select>

    <select id="selectByIdAndTenant" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
    </select>

    <select id="selectByMac" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE mac_address = #{macAddress}
    </select>

    <select id="selectByMacAndTenant" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE mac_address = #{macAddress}
          AND tenant_id = #{tenantId}
    </select>

    <select id="selectByNormalizedMac" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE LOWER(REPLACE(REPLACE(REPLACE(mac_address, ':', ''), '-', ''), ' ', '')) = #{normalizedMac}
        LIMIT 1
    </select>

    <select id="selectByNormalizedMacAndTenant" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE LOWER(REPLACE(REPLACE(REPLACE(mac_address, ':', ''), '-', ''), ' ', '')) = #{normalizedMac}
          AND tenant_id = #{tenantId}
        LIMIT 1
    </select>

    <insert id="upsertByMac" parameterType="com.cd.entity.HostEntity">
        INSERT INTO hosts (tenant_id, hostname, ipv4, mac_address, os_name, os_version, os_arch, os_release,
                           cpu_model, cpu_physical_cores, cpu_logical_cores,
                           mem_total, mem_used, mem_available, mem_usage, status, last_scan_time)
        VALUES (COALESCE(#{tenantId}, 0), #{hostname}, #{ipv4}, #{macAddress}, #{osName}, #{osVersion}, #{osArch}, #{osRelease},
                #{cpuModel}, #{cpuPhysicalCores}, #{cpuLogicalCores},
                #{memTotal}, #{memUsed}, #{memAvailable}, #{memUsage}, #{status}, #{lastScanTime})
        ON DUPLICATE KEY UPDATE
            hostname = VALUES(hostname),
            ipv4 = VALUES(ipv4),
            os_name = VALUES(os_name),
            os_version = VALUES(os_version),
            os_arch = VALUES(os_arch),
            os_release = VALUES(os_release),
            cpu_model = VALUES(cpu_model),
            cpu_physical_cores = VALUES(cpu_physical_cores),
            cpu_logical_cores = VALUES(cpu_logical_cores),
            mem_total = VALUES(mem_total),
            mem_used = VALUES(mem_used),
            mem_available = VALUES(mem_available),
            mem_usage = VALUES(mem_usage),
            status = VALUES(status),
            last_scan_time = COALESCE(VALUES(last_scan_time), last_scan_time),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="heartbeatByMac">
        INSERT INTO hosts (tenant_id, mac_address, status, updated_at)
        VALUES (0, #{macAddress}, 1, NOW())
        ON DUPLICATE KEY UPDATE
            status = 1,
            updated_at = NOW()
    </insert>

    <update id="markOffline">
        UPDATE hosts
        SET status = 0,
            updated_at = updated_at
        WHERE status = 1
          AND updated_at &lt; DATE_SUB(NOW(), INTERVAL #{seconds} SECOND)
    </update>

    <update id="reconcileStatusByHeartbeat">
        UPDATE hosts
        SET status = CASE WHEN updated_at >= DATE_SUB(NOW(), INTERVAL #{seconds} SECOND) THEN 1 ELSE 0 END,
            updated_at = updated_at
        WHERE status &lt;&gt; CASE WHEN updated_at >= DATE_SUB(NOW(), INTERVAL #{seconds} SECOND) THEN 1 ELSE 0 END
    </update>

    <update id="updateLastScanTimeByMac">
        UPDATE hosts
        SET last_scan_time = #{lastScanTime},
            updated_at = updated_at
        WHERE mac_address = #{macAddress}
    </update>

    <update id="updateLastScanTimeById">
        UPDATE hosts
        SET last_scan_time = #{lastScanTime},
            updated_at = updated_at
        WHERE id = #{id}
    </update>

    <select id="selectAutoProbeCandidates" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE status = 1
        ORDER BY COALESCE(last_scan_time, '1970-01-01') ASC, id ASC
        LIMIT #{limit}
    </select>

    <select id="selectPortScanCandidates" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts h
        WHERE h.status = 1
        ORDER BY COALESCE(
            (SELECT MAX(created_at) FROM port_scan_results WHERE mac_address = h.mac_address AND deleted = 0),
            '1970-01-01'
        ) ASC, h.id ASC
        LIMIT #{limit}
    </select>

    <select id="selectAllIds" resultType="long">
        SELECT id
        FROM hosts
        ORDER BY id ASC
    </select>

    <select id="selectAllIdsByTenant" resultType="long">
        SELECT id
        FROM hosts
        WHERE tenant_id = #{tenantId}
        ORDER BY id ASC
    </select>

    <select id="selectPage" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        <include refid="keywordWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectPageByTenant" resultMap="HostEntityResultMap">
        SELECT <include refid="baseColumns"/>
        FROM hosts
        WHERE tenant_id = #{tenantId}
        <include refid="keywordAnd"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(1) FROM hosts
        <include refid="keywordWhere"/>
    </select>

    <select id="countAllByTenant" resultType="long">
        SELECT COUNT(1)
        FROM hosts
        WHERE tenant_id = #{tenantId}
        <include refid="keywordAnd"/>
    </select>

    <select id="countByTenantId" resultType="long">
        SELECT COUNT(1)
        FROM hosts
        WHERE tenant_id = #{tenantId}
    </select>
</mapper>

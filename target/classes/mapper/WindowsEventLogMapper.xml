<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.WindowsEventLogMapper">

    <insert id="insertBatchIgnore">
        INSERT IGNORE INTO windows_event_logs
            (host_id, log_type, event_id, event_time,
             username, level, message, record_number, raw_json)
        VALUES
        <foreach collection="list" item="it" separator=",">
            (#{it.hostId}, #{it.logType}, #{it.eventId}, #{it.eventTime},
             #{it.username}, #{it.level}, #{it.message}, #{it.recordNumber}, #{it.rawJson})
        </foreach>
    </insert>

    <select id="selectIdsByKeys" resultType="com.cd.entity.WindowsEventLogEntity">
        SELECT id, host_id AS hostId, log_type AS logType, record_number AS recordNumber
        FROM windows_event_logs
        WHERE (host_id, log_type, record_number) IN
        <foreach collection="list" item="it" open="(" separator="," close=")">
            (#{it.hostId}, #{it.logType}, #{it.recordNumber})
        </foreach>
    </select>
</mapper>

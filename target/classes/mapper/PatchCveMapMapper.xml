<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.PatchCveMapMapper">

    <resultMap id="PatchCveMapResultMap" type="com.cd.entity.PatchCveMapEntity">
        <id column="id" property="id"/>
        <result column="patch_id" property="patchId"/>
        <result column="cve_id" property="cveId"/>
        <result column="vendor" property="vendor"/>
        <result column="product" property="product"/>
        <result column="affected_version_range" property="affectedVersionRange"/>
        <result column="fixed_version" property="fixedVersion"/>
        <result column="fix_type" property="fixType"/>
        <result column="exploit_status" property="exploitStatus"/>
        <result column="kev_flag" property="kevFlag"/>
        <result column="cvss_score" property="cvssScore"/>
        <result column="severity" property="severity"/>
        <result column="reference_url" property="referenceUrl"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, patch_id, cve_id, vendor, product, affected_version_range, fixed_version,
        fix_type, exploit_status, kev_flag, cvss_score, severity, reference_url, created_at, updated_at
    </sql>

    <sql id="listWhere">
        <where>
            <if test="keyword != null and keyword != ''">
                (
                    patch_id LIKE CONCAT('%', #{keyword}, '%')
                    OR cve_id LIKE CONCAT('%', #{keyword}, '%')
                    OR product LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="severity != null and severity != ''">
                AND severity = #{severity}
            </if>
            <if test="kevFlag != null">
                AND kev_flag = #{kevFlag}
            </if>
        </where>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.PatchCveMapEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO patch_cve_map (
            patch_id, cve_id, vendor, product, affected_version_range, fixed_version,
            fix_type, exploit_status, kev_flag, cvss_score, severity, reference_url
        ) VALUES (
            #{patchId}, #{cveId}, #{vendor}, #{product}, #{affectedVersionRange}, #{fixedVersion},
            #{fixType}, #{exploitStatus}, #{kevFlag}, #{cvssScore}, #{severity}, #{referenceUrl}
        )
    </insert>

    <update id="updateById" parameterType="com.cd.entity.PatchCveMapEntity">
        UPDATE patch_cve_map
        SET patch_id = #{patchId},
            cve_id = #{cveId},
            vendor = #{vendor},
            product = #{product},
            affected_version_range = #{affectedVersionRange},
            fixed_version = #{fixedVersion},
            fix_type = #{fixType},
            exploit_status = #{exploitStatus},
            kev_flag = #{kevFlag},
            cvss_score = #{cvssScore},
            severity = #{severity},
            reference_url = #{referenceUrl}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM patch_cve_map WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM patch_cve_map
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectById" resultMap="PatchCveMapResultMap">
        SELECT <include refid="baseColumns"/>
        FROM patch_cve_map
        WHERE id = #{id}
    </select>

    <select id="selectByPatchIdAndCveId" resultMap="PatchCveMapResultMap">
        SELECT <include refid="baseColumns"/>
        FROM patch_cve_map
        WHERE patch_id = #{patchId}
          AND cve_id = #{cveId}
        LIMIT 1
    </select>

    <select id="selectPage" resultMap="PatchCveMapResultMap">
        SELECT <include refid="baseColumns"/>
        FROM patch_cve_map
        <include refid="listWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(1)
        FROM patch_cve_map
        <include refid="listWhere"/>
    </select>
</mapper>

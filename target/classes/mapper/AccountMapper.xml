<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.mapper.AccountMapper">

    <resultMap id="AccountResultMap" type="com.cd.entity.AccountEntity">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="task_id" property="taskId"/>
        <result column="host_name" property="hostName"/>
        <result column="mac_address" property="macAddress"/>
        <result column="source" property="source"/>
        <result column="asset_count" property="assetCount"/>
        <result column="asset_json" property="assetJson"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, tenant_id, task_id, host_name, mac_address, source, asset_count, asset_json, deleted, created_at, updated_at
    </sql>

    <sql id="listWhere">
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <sql id="listWhereByTenant">
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        <if test="keyword != null and keyword != ''">
            AND (host_name LIKE CONCAT('%', #{keyword}, '%')
                 OR mac_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="macAddress != null and macAddress != ''">
            AND mac_address = #{macAddress}
        </if>
    </sql>

    <insert id="insert" parameterType="com.cd.entity.AccountEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (tenant_id, task_id, host_name, mac_address, source, asset_count, asset_json)
        VALUES (COALESCE(#{tenantId}, 0), #{taskId}, #{hostName}, #{macAddress}, #{source}, #{assetCount}, #{assetJson})
    </insert>

    <select id="selectById" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByIdAndTenant" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </select>

    <select id="selectPage" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts
        <include refid="listWhere"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectPageByTenant" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts
        <include refid="listWhereByTenant"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countFiltered" resultType="long">
        SELECT COUNT(1) FROM accounts
        <include refid="listWhere"/>
    </select>

    <select id="countFilteredByTenant" resultType="long">
        SELECT COUNT(1) FROM accounts
        <include refid="listWhereByTenant"/>
    </select>

    <update id="softDeleteById">
        UPDATE accounts SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

    <update id="softDeleteByIdAndTenant">
        UPDATE accounts
        SET deleted = 1
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

    <update id="updateAssetJsonById">
        UPDATE accounts
        SET asset_json = #{assetJson},
            asset_count = #{assetCount}
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateAssetJsonByIdAndTenant">
        UPDATE accounts
        SET asset_json = #{assetJson},
            asset_count = #{assetCount}
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

    <!-- 每个 MAC 最新一条记录（用于总览聚合） -->
    <select id="selectLatestPerMac" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts a
        WHERE a.deleted = 0
          AND a.id = (SELECT MAX(id) FROM accounts WHERE mac_address = a.mac_address AND deleted = 0)
    </select>

    <select id="selectLatestPerMacByTenant" resultMap="AccountResultMap">
        SELECT <include refid="baseColumns"/>
        FROM accounts a
        WHERE a.deleted = 0
          AND a.tenant_id = #{tenantId}
          AND a.id = (
              SELECT MAX(id)
              FROM accounts
              WHERE mac_address = a.mac_address
                AND tenant_id = #{tenantId}
                AND deleted = 0
          )
    </select>

    <select id="selectLatestRiskSnapshotByHostIds" resultType="com.cd.dto.AccountRiskSnapshotDTO">
        SELECT h.id AS hostId,
               a.id AS accountId,
               a.mac_address AS macAddress,
               a.asset_json AS assetJson
        FROM hosts h
        JOIN accounts a
          ON LOWER(REPLACE(REPLACE(REPLACE(a.mac_address, ':', ''), '-', ''), ' ', ''))
           = LOWER(REPLACE(REPLACE(REPLACE(h.mac_address, ':', ''), '-', ''), ' ', ''))
        WHERE h.id IN
        <foreach collection="hostIds" item="it" open="(" separator="," close=")">
            #{it}
        </foreach>
          AND a.deleted = 0
          AND a.id = COALESCE(
              (
                  SELECT MAX(a2.id)
                  FROM accounts a2
                  WHERE a2.deleted = 0
                    AND LOWER(REPLACE(REPLACE(REPLACE(a2.mac_address, ':', ''), '-', ''), ' ', ''))
                        = LOWER(REPLACE(REPLACE(REPLACE(h.mac_address, ':', ''), '-', ''), ' ', ''))
                    AND a2.asset_json LIKE '%"risk_level"%'
              ),
              (
                  SELECT MAX(a3.id)
                  FROM accounts a3
                  WHERE a3.deleted = 0
                    AND LOWER(REPLACE(REPLACE(REPLACE(a3.mac_address, ':', ''), '-', ''), ' ', ''))
                        = LOWER(REPLACE(REPLACE(REPLACE(h.mac_address, ':', ''), '-', ''), ' ', ''))
              )
          )
    </select>
</mapper>

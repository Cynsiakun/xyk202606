1. 主机视角的资产清单（中间件、数据库、大数据组件、web应用、web框架、web站点等资产），参考goby的界面和功能设计
	1.1 端口及服务（探测开放了哪些端口）
	1.2重点还是规则（平台端）
	1.3 客户端进行扫描
2. 基线核查加固的升级（web中间件/数据库），多版本的基线核查
	2.1基线的规则扩大，涉及web中间件/数据库/缓存等等
	2.2等级保护：等保2.0
		等保分了级别1-5级
		一般都是从3级开始做
不同的等保级别，基线的规则是不一样的。比如，密码的修改时间间隔，3级：90天，4级：50天，5级：20天


一、当前自查结论

数据库连接：127.0.0.1:3306，root/root，当前库为 application-dev.yml 中的 xyk2026。

已存在的基线相关表：
- baseline_asset_type：资产类型字典，已有 OS_WINDOWS、MW_TOMCAT、DB_MYSQL、CACHE_REDIS 等类型。
- baseline_protection_level：等保/安全防护等级字典，已有 S1A2G2、S2A2G2、S3A3G3、S3A4G3、L3、L4、L5。
- baseline_rule_asset_ref：规则和资产类型的关联表。
- baseline_rule：已有 protection_level_flag、group_combine_logic 字段，但 Java 实体和 Mapper 还没有映射。
- baseline_rule_item：已有 protection_level_id、value_type、value_set、logic_group、group_operator 字段，但 Java 实体和 Mapper 还没有映射。
- baseline_task：已有 protection_level_id、target_level、asset_type_filter 字段，但创建任务 DTO/服务/前端还没有贯通。
- baseline_result：已有 asset_type_id、protection_level_id、item_id 字段，但规则引擎落库还没有写入。
- baseline_summary：已有 asset_type_id、protection_level_id 字段，但汇总逻辑还没有按新维度统计。

当前先跑通用的三条等级保护样例规则：
- WIN-LEV-01：等保密码修改时间间隔，OS_WINDOWS，L3<=90、L4<=50、L5<=20。
- MW-TC-LEV-01：等保Tomcat会话超时检查，MW_TOMCAT，L3<=30、L4<=20、L5<=15。
- DB-MY-LEV-01：等保MySQL密码过期策略检查，DB_MYSQL，L3<=90、L4<=50、L5<=20。

现状差距：
- 平台下发 baseline_scan 时仍然只按 ruleIds 下发全部 items，没有按 protection_level_id 过滤。
- 通信格式应尽量保持不变：客户端不理解等保等级，只执行平台下发的检查项并回传实际值。
- 客户端现在按 OS 合规项执行，后续只需要增加 Tomcat/MySQL/Redis 等采集能力，不参与 PASS/FAIL 判定。
- 结果回传主要是 item 粒度结果，平台侧尚未把 item_id、asset_type_id、protection_level_id 写入结果表。
- 新增的逻辑组字段还没被规则引擎使用，当前仍是一条 item 一条结果。


二、平台端任务（Server + 前端）

TODO-Server-1：打通 V5 新字段映射
- [x] BaselineRuleEntity 增加 assetType、level、protectionLevelFlag、groupCombineLogic。
- [x] BaselineRuleItemEntity 增加 protectionLevelId、valueType、valueSet、logicGroup、groupOperator。
- [x] BaselineTaskEntity / BaselineResultEntity / BaselineSummaryEntity 补齐 protectionLevelId、assetTypeId、itemId 等字段。
- [x] BaselineRuleMapper.xml 的 select/resultMap 补齐上述字段，避免数据库有字段但 Java 侧取不到。

TODO-Server-2：任务创建增加等保等级和资产类型过滤
- [x] BaselineTaskCreateRequestDTO 增加 protectionLevelCode 或 protectionLevelId、assetTypeCodes。
- [x] 新建任务时写入 baseline_task.protection_level_id，并把 asset_type_filter 写成逗号分隔的资产类型编码。
- [x] 加载规则时根据 assetTypeCodes 关联 baseline_rule_asset_ref 过滤规则。
- [x] 加载规则项时根据 protectionLevelId 只下发：protection_level_id IS NULL 或 protection_level_id = 当前等级。
- [x] rule_scope/rule_snapshot_json 中记录本次任务的 protectionLevel、assetTypes、ruleIds，方便审计和复检。

TODO-Server-3：下发格式轻量化与兼容
- [x] baseline_scan 根节点不下发 protectionLevel / protectionLevelId，客户端不感知 L3/L4/L5。
- [x] 平台在下发前完成等级过滤：只下发 protection_level_id IS NULL 或等于任务 protection_level_id 的 item。
- [x] checks[].items[] 保持现有 itemId/checkKey/operator/expectedValue/matchType/valueType 结构；operator/expectedValue 仅用于兼容，最终判定仍由平台端完成。
- [x] 如客户端执行路由需要，可在 checks[] 保留 checkMethod/checkScript/osType；不引入需要客户端理解的等级字段。
- [x] 数据库/中间件采集所需连接信息先走平台配置或客户端本地配置，不在规则协议中硬塞 credential。

TODO-Server-4：结果入库和规则引擎升级
- [x] BaselineCheckDataService 接收新旧两种 baseline_scan_result，先保持兼容。
- [x] BaselineRuleEngineImpl 生成 baseline_result 时写 item_id、asset_type_id、protection_level_id。
- [x] 根据 value_type 做 NUMBER/STRING/BOOLEAN/ENUM 比较，value_set 非空时支持 IN/NOT_IN。
- [ ] 使用 logic_group、group_operator、group_combine_logic 做规则级聚合；第一阶段可以仍保留 item 明细，同时增加 ruleStatus 汇总。
- [ ] baseline_summary 按 host + task + asset_type + protection_level 维度写统计，原有总览继续保留。
- [ ] 复检、工单、修复状态继承逻辑要带上 assetType/protectionLevel/itemId，避免不同等级结果串线。

TODO-Server-4 实施备注：
- 已兼容当前客户端回传的 results[] 格式；如果 itemId 缺失，会用 ruleId + checkKey + 当前任务 protection_level_id 兜底反查规则项。
- baseline_result 已写入 item_id、asset_type_id、protection_level_id，结果页可按这些维度展示/筛选。
- baseline_summary 当前库唯一键仍是 uk_host_task(host_id, task_id)，暂不能安全写多条资产/等级维度汇总；本轮继续保留 host+task 总览，资产维度统计先写入 task_host.result_summary 的 assetTypeSummary。

TODO-Server-5：规则库和管理页面
- [x] 基线规则列表增加资产类型、等保等级筛选。
- [x] 新建任务弹窗增加等保等级选择，默认 L3；增加资产类型筛选，先支持 OS_WINDOWS、MW_TOMCAT、DB_MYSQL。
- [x] 规则选择列表展示 ruleCode、assetType、protectionLevelFlag、category。
- [x] 结果页面增加资产类型、等保等级列，支持按资产类型查看失败项。
- [x] 提供 GET /api/baseline/protection-levels 和 GET /api/baseline/asset-types，供前端下拉框使用。
- [ ] 当前先以三条样例规则跑通，不急着导入完整 web中间件/数据库/缓存规则库。

TODO-Server-5 实施备注：
- 已完成规则管理页筛选、任务弹窗规则范围选择、规则选项接口过滤、字典接口和结果页资产类型筛选入口。
- 结果页资产类型/等保等级列优先读取 baseline_result.asset_type_id、protection_level_id；在 TODO-Server-4 未写入这些字段前，旧结果可能显示为 “-”，但按规则资产关联过滤已先兼容。

TODO-Server-6：数据脚本和验证
- [ ] 检查 V5_baseline_multilevel.sql 在重复执行时是否会因 ADD COLUMN 重复报错，必要时拆成幂等迁移。
- [ ] 为三条样例规则补充清晰的 asset_ref 和 L3/L4/L5 item 数据。
- [ ] 增加最小链路测试：创建 L3/L4/L5 任务，确认同一规则下发的 expectedValue 不同。
- [ ] 增加规则引擎测试：MySQL default_password_lifetime=60 时，L3 PASS、L4 FAIL、L5 FAIL。


三、客户端任务（Agent）

TODO-Agent-1：保持协议兼容和任务路由
- [ ] 继续按现有 baseline_scan 格式执行，不新增等保等级解析逻辑。
- [ ] 根据 checkMethod/checkScript/checkKey 分发到不同采集器：Windows、Tomcat、MySQL，后续扩 Redis/Nginx/IIS。
- [ ] 客户端只采集 actualValue / evidence / executeStatus，不根据 expectedValue 判定 PASS/FAIL。

TODO-Agent-2：目标资产发现和实例定位
- [ ] 复用端口扫描/资产清单结果识别 8080/8443 的 Tomcat、3306 的 MySQL、6379 的 Redis。
- [ ] 客户端从本机资产清单、本地配置或已识别服务中推断目标实例。
- [ ] MySQL 第一阶段支持 host=127.0.0.1、port=3306、username/password 或 credentialRef。
- [ ] Tomcat 第一阶段支持读取 server.xml/web.xml/context.xml，或由平台下发 configPath。

TODO-Agent-3：采集器先跑通三类规则
- [ ] Windows：读取 MaximumPasswordAge，返回 WIN-LEV-01 需要的 actualValue。
- [ ] Tomcat：读取 session-timeout，返回 session_timeout。
- [ ] MySQL：连接实例执行 SHOW VARIABLES LIKE 'default_password_lifetime'，返回 default_password_lifetime。
- [ ] 采集失败时 executeStatus=ERROR，并返回 message/evidence，不在客户端硬判 PASS/FAIL，优先由平台统一比较。

TODO-Agent-4：结果回传增强
- [ ] 每条 results[] 回传 ruleId、itemId、ruleVersion、checkKey、actualValue、executeStatus、message、evidence。
- [ ] 可选回传 targetId/targetName，方便平台排查实例；不回传 protectionLevel。
- [ ] scanSummary 中回传 total/pass/fail/error 可选；平台仍以规则引擎结果为准。
- [ ] 严格保证 taskId、taskHostId、hostId 原样透传。

TODO-Agent-5：安全和配置
- [ ] 数据库密码不直接写死在规则里，先支持平台配置或本地 Agent 配置，协议中用 credentialRef 表示。
- [ ] evidence 避免回传明文密码、连接串等敏感信息。
- [ ] 对 MySQL/Tomcat 采集设置超时，避免一个实例卡住整个 baseline_scan。


四、通信格式约定（轻量版）

原则：
- 客户端不感知等保等级，不理解 L3/L4/L5。
- 平台端根据 baseline_task.protection_level_id 选择规则项：protection_level_id = 当前等级，或 protection_level_id IS NULL 的通用规则。
- 通信格式尽量沿用现有合规基线协议；客户端只负责采集，平台端负责判定。

4.1 平台 -> 客户端：baseline_scan 保持现有格式

```json
{
  "type": "baseline_scan",
  "taskId": 42,
  "taskHostId": 501,
  "hostId": 10,
  "macAddress": "00:1A:2B:3C:4D:5E",
  "createdAt": "2026-06-17T21:30:00+08:00",
  "checks": [
    {
      "ruleId": 150,
      "ruleCode": "DB-MY-LEV-01",
      "ruleVersion": 1,
      "checkMethod": "SCRIPT",
      "checkScript": null,
      "items": [
        {
          "itemId": 3001,
          "checkKey": "default_password_lifetime",
          "operator": "<=",
          "expectedValue": "90",
          "matchType": "EXACT",
          "valueType": "NUMBER"
        }
      ]
    }
  ]
}
```

字段说明：
- type：固定 baseline_scan。
- checks[].items[]：已经由平台按等保等级筛选完成，客户端无需知道该 item 属于 L3/L4/L5。
- operator/expectedValue/matchType/valueType：保留是为了兼容现有客户端和便于调试；客户端不做最终合规判定。
- 数据库/中间件/缓存采集需要的实例信息，第一阶段由客户端本地配置或资产清单推断，不在 baseline_scan 中引入 targets。

4.2 客户端 -> 平台：baseline_scan_result 保持采集结果格式

```json
{
  "type": "baseline_scan_result",
  "taskId": 42,
  "taskHostId": 501,
  "hostId": 10,
  "macAddress": "00:1A:2B:3C:4D:5E",
  "scanTime": "2026-06-17T21:31:00+08:00",
  "results": [
    {
      "ruleId": 150,
      "ruleCode": "DB-MY-LEV-01",
      "ruleVersion": 1,
      "itemId": 3001,
      "checkKey": "default_password_lifetime",
      "actualValue": "60",
      "executeStatus": "SUCCESS",
      "message": "采集成功",
      "evidence": "SHOW VARIABLES LIKE 'default_password_lifetime' => 60"
    }
  ],
  "scanSummary": {
    "total": 1,
    "success": 1,
    "error": 0
  }
}
```

字段说明：
- executeStatus：SUCCESS/ERROR。PASS/FAIL 由平台规则引擎计算，客户端先只负责采集。
- actualValue：原始配置值，尽量保持可比较的标量，比如 60、ON、OFF、true。
- evidence：采集证据，不允许包含密码、Token、完整连接串。
- targetId/targetName：可选字段；需要区分多个实例时再回传。

兼容要求：
- 平台继续兼容旧版 checkData 对象格式，但新客户端统一使用 results[]。
- 老任务没有 protection_level_id 时，平台按默认 L3 或通用规则处理。


五、建议执行顺序

TODO-Phase-1：先跑通三条样例规则
- [ ] 平台端补齐实体/Mapper/DTO 字段。
- [ ] 新建任务时选择 L3/L4/L5，并确认下发 expectedValue 分别为 90/50/20。
- [ ] 客户端实现 Windows、Tomcat、MySQL 三个最小采集器，只采集和回传 actualValue。
- [ ] 回传 results[] 后，平台能生成 baseline_result，并写入 item_id/protection_level_id/asset_type_id。

TODO-Phase-2：补齐页面和查询
- [ ] 任务列表和结果详情展示等保等级、资产类型。
- [ ] 规则管理增加资产类型/等保等级筛选。
- [ ] 汇总统计按资产类型和等保等级聚合。

TODO-Phase-3：扩规则库
- [ ] 在三条样例规则稳定后，再导入更多 MySQL/Tomcat/Redis/Nginx/IIS 规则。
- [ ] 每类资产先选 5-10 条高价值规则，保证字段命名和客户端 checkKey 一致。
- [ ] 再考虑修复加固动作，第一阶段只做核查和结果闭环。
